cd /path/to/your/tiktok-app
    Then, install the necessary Python libraries:
```bash
pip install streamlit pandas beautifulsoup4 lxml
4.  **Run the App:** In the same Terminal window, run this command:
```bash
streamlit run app.py
    Your web browser will automatically open, and you can start uploading your `.html` files to test it!# content-gap-analyser
# content-gap-analyser
